╔════════════════════════════════════════════════════════════════╗
║          NGINX SETUP - QUICK REFERENCE CARD                    ║
║          Bot Monitor @ www.safebox.cfd/botm/***                ║
╚════════════════════════════════════════════════════════════════╝

┌─────────────────────────────────────────────────────────────────┐
│ 🚀 QUICK SETUP                                                  │
└─────────────────────────────────────────────────────────────────┘

  1. Run automated setup:
     $ sudo bash setup-nginx.sh

  2. Start your Node.js app:
     $ npm start
     (or use PM2/systemd for production)

  3. Access your app:
     http://www.safebox.cfd/botm/

┌─────────────────────────────────────────────────────────────────┐
│ ⚙️  CONFIGURATION                                               │
└─────────────────────────────────────────────────────────────────┘

  App Port:     3000 (from .env)
  Nginx Config: /etc/nginx/sites-available/botm
  Domain:       www.safebox.cfd
  Base Path:    /botm/

┌─────────────────────────────────────────────────────────────────┐
│ 🔧 COMMON COMMANDS                                              │
└─────────────────────────────────────────────────────────────────┘

  Test config:    sudo nginx -t
  Reload:         sudo systemctl reload nginx
  Restart:        sudo systemctl restart nginx
  Status:         sudo systemctl status nginx
  Error logs:     sudo tail -f /var/log/nginx/error.log
  Access logs:    sudo tail -f /var/log/nginx/access.log

┌─────────────────────────────────────────────────────────────────┐
│ 🔒 ENABLE HTTPS (Recommended)                                   │
└─────────────────────────────────────────────────────────────────┘

  1. Install Certbot:
     $ sudo apt install certbot python3-certbot-nginx

  2. Get SSL certificate:
     $ sudo certbot --nginx -d safebox.cfd -d www.safebox.cfd

  3. Follow prompts - Certbot will auto-configure HTTPS

┌─────────────────────────────────────────────────────────────────┐
│ 🧪 TESTING                                                      │
└─────────────────────────────────────────────────────────────────┘

  Check if app is running:
  $ ps aux | grep node
  $ sudo netstat -tlnp | grep 3000

  Test endpoint:
  $ curl http://localhost:3000
  $ curl http://www.safebox.cfd/botm/

┌─────────────────────────────────────────────────────────────────┐
│ 📁 FILES CREATED                                                │
└─────────────────────────────────────────────────────────────────┘

  ✓ nginx-botm.conf     - Nginx configuration file
  ✓ setup-nginx.sh      - Automated setup script
  ✓ NGINX_SETUP.md      - Detailed documentation
  ✓ QUICK_REFERENCE.txt - This file

┌─────────────────────────────────────────────────────────────────┐
│ 🔥 TROUBLESHOOTING                                              │
└─────────────────────────────────────────────────────────────────┘

  502 Bad Gateway:
  → Check if Node.js app is running
  → Verify port 3000 is listening
  → Check firewall settings

  404 Not Found:
  → Verify Nginx config is enabled
  → Check /botm/ path in URL
  → Review Nginx error logs

  WebSocket issues:
  → Check browser console
  → Verify /botm/socket.io/ location block
  → Check CORS settings in app

┌─────────────────────────────────────────────────────────────────┐
│ 📋 CHECKLIST                                                    │
└─────────────────────────────────────────────────────────────────┘

  ☐ Nginx installed
  ☐ Configuration file copied
  ☐ Site enabled in sites-enabled
  ☐ Nginx configuration tested (nginx -t)
  ☐ Nginx reloaded
  ☐ Node.js app running on port 3000
  ☐ DNS pointing to server IP
  ☐ Firewall allows HTTP (80) and HTTPS (443)
  ☐ SSL certificate installed (optional but recommended)
  ☐ App accessible at www.safebox.cfd/botm/

┌─────────────────────────────────────────────────────────────────┐
│ 🎯 PRODUCTION RECOMMENDATIONS                                   │
└─────────────────────────────────────────────────────────────────┘

  1. Use PM2 to manage Node.js app:
     $ npm install -g pm2
     $ pm2 start index.js --name botm
     $ pm2 startup
     $ pm2 save

  2. Enable HTTPS with Let's Encrypt

  3. Set up firewall:
     $ sudo ufw allow 'Nginx Full'
     $ sudo ufw enable

  4. Configure log rotation

  5. Set up monitoring (PM2 monitoring, uptime checks)

╔════════════════════════════════════════════════════════════════╗
║  For detailed instructions, see NGINX_SETUP.md                 ║
╚════════════════════════════════════════════════════════════════╝
